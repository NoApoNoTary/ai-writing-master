# Voice Preset v0.3 验收报告

> 日期：2026-07-28
> PRD：`docs/proposals/2026-07-28-voice-preset-v0.3-prd.md`
> 结论：**PASS**

## 交付范围

- Runtime：`src/writing_master/voice_presets.py`
- CLI：`writing-master voice list|snapshot|verify-run`
- Registry：`src/writing_master/voice_profiles/registry.json`
- 工作流合同：`skills/writing-master/references/voice-presets.md` 及主 Skill、角色卡、Handoff、审校、Personal Context、Rewrite 接入
- 自动测试：`tests/test_voice_presets.py`、`tests/test_voice_cli.py`、`tests/test_voice_workflow_contract.py` 及既有测试扩展
- 模板实验：`experiments/voice-presets/khazix0918/`
- 真实旧稿 Pilot：`experiments/voice-presets/khazix0918/pilot/`
- 固定内容包 Benchmark：`docs/goals/2026-07-28-v0.3-voice-benchmark/`

## FR 验收矩阵

| 要求 | 证据 | 结果 |
|---|---|---|
| FR-1 领域边界 | Voice 独立 Registry/Snapshot；Personal Context 文档与学习路径不接收 Voice Profile | PASS |
| FR-2 选择入口 | 内容契约默认 `natural-default`；CLI 支持序号、ID、显示名称；未知项返回可用列表 | PASS |
| FR-3 内置范围 | Registry 精确包含自然默认与 3 个非默认 Profile；固定内容包证明至少 5 个维度可辨识 | PASS |
| FR-4 Profile Contract | Runtime 校验 schema、ID、version、scope、10 个 voice 字段、固定 preserve 与紧凑合成示例 | PASS |
| FR-5 任务快照 | write-once Snapshot、canonical hash、同 Voice 幂等、不同 Voice 冲突、缺失/篡改阻断、Registry-independent resume | PASS |
| FR-6 读取边界 | Quick/Standard 合同只在 Phase 3/Voice Audit 读取；Deep Handoff 仅 Writer/Auditor 自动注入并校验 | PASS |
| FR-7 生成边界 | Writer/Voice 合同明确内容优先级与禁止新增事实、经历、数字、引述和身份声明 | PASS |
| FR-8 Voice Audit | issue 必须包含位置、Profile 规则、正文证据和修订边界；Pilot Voice Audit 通过 | PASS |
| FR-9 状态与恢复 | status 记录 ID/version/state/hash；支持 ready、pending、legacy-natural、unavailable；旧任务不回填 | PASS |
| FR-10 选择变更 | Snapshot 后不同 Voice 返回 `snapshot_conflict`；工作流要求创建关联新任务 | PASS |
| FR-11 学习隔离 | `learn propose --run-dir` 校验来源任务；非默认 Voice 返回 `learning_isolated` | PASS |
| FR-12 Rewrite | Rewrite 保持 canonical source 已验收声音，不增加 Voice Selector | PASS |
| FR-13 失败语义 | 显式非默认失败阻断；自然默认异常记录 unavailable；Snapshot/status/hash 异常不降级 | PASS |
| FR-14 模板生产 | Nuwa 与 benchmark 仅用于离线实验；运行时无网络、外部 Skill 或第三方依赖 | PASS |
| FR-15 Khazix Pilot | Article-only Corpus 8 training + 4 holdout；READY gate、旧稿副本、三类回归、对比与 promote 决定齐全 | PASS |

## Pilot 证据

- 原 canonical `final.md` 与 `pilot/baseline.md` SHA-256 均为 `0601c5a1fd9e78416a28032ca65b46518efac5f0debe0c82a7c1b4c0afe26b7d`。
- `voice-profile.json` 文件 SHA-256：`fa560413f580652775fff6a20a1c38d8ea06c05880ceb4de1d0347e9c548f4ed`。
- `voice-profile.json` canonical JSON SHA-256：`44e921661c17d347a03993bc0209243d1ada317c4dd4710d85ca5abc28378e66`。
- 改写 SHA-256：`60a0e44767903fa6c1f28ac76b4dbbe7b9543483f81414a0b70829ec0b502602`。
- 15 个 citation marker 的 multiset 与 5 项来源均保持；Evidence、Editorial、Voice 回归均 PASS。
- Pilot 决定：`promote` 到 candidate-registry review，不作为对外发布或真实作者身份产品项。

## Release Benchmark

固定 Brief、accepted claims、Editorial Brief 与用户材料生成四个版本。四个版本保持相同数字、故障原因、核心判断、作者立场与唯一第一人称事实；三个非默认 Profile 在 register、句式节奏、段落、开场、转场、确定性、幽默、类比、词汇和 pacing 中均形成可解释差异。结论：四个内置 Profile 可进入 v0.3 Registry。

## 自动验证

```text
PYTHONPATH=src python3 -m unittest discover -s tests -q
Ran 196 tests ... OK

PYTHONPATH=src python3 -m compileall -q src tests
bash -n install.sh
git diff --check
uv build --out-dir /tmp/awm-dist
./bin/writing-master voice list --json
```

Wheel 已验证包含 `writing_master/voice_profiles/registry.json`；Runtime 仍为 Python 标准库零第三方依赖。

## 发布结论

Voice Preset v0.3 的选择、Registry、Snapshot、状态、恢复、角色隔离、Voice Audit、学习隔离、Rewrite 边界、真实 Pilot 与固定内容包 Benchmark 均已形成可复查证据。实验 Profile 只晋级候选评审；正式内置 Registry 仍是四个描述性声音。
