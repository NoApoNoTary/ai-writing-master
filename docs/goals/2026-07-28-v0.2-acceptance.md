# Goal A 验收记录：Personal Context Foundation

- Goal ID：`AWM-PC-A-20260728`
- 状态：`complete`
- 创建日期：2026-07-28
- 最后更新：2026-07-28
- Goal 合同：[2026-07-28-v0.2-goal-contract.md](2026-07-28-v0.2-goal-contract.md)
- 执行清单：[2026-07-28-v0.2-execution-checklist.md](../plans/2026-07-28-v0.2-execution-checklist.md)

> 本文件只记录证据。纯 evidence/log/checkbox 更新不触发代码重测；若更新同时改变合同语义，则必须重跑受影响验证。

## 1. Pre-delivery Baseline

以下是生成本次计划文件之前的仓库基线；当前 `docs/plans/` 和 `docs/goals/` 是本次交付新增文件，不属于预存 dirty files。

| 项目 | 2026-07-28 基线 |
|---|---|
| Repository | `/home/amose/ai-writing-master` |
| Branch | `codex/writing-workflow-maintenance` |
| HEAD | `61dec8cf1e384c752f13a007a1283e9df82fdb95` |
| Main | `fab9fffcf38c537772dfc8a8b71344a7d33830e5` |
| Pre-existing dirty file | `?? docs/proposals/2026-07-24-opus5-retrospective-plan.html` |
| Package version | `1.0.0` |
| Unit tests | 51 tests, pass |
| CLI commands | `quality`、`similarity`、`handoff`、`home` |
| Runtime dependencies | Python 标准库 |

### Protected file fingerprint

```text
path: docs/proposals/2026-07-24-opus5-retrospective-plan.html
sha256: 503ac7b0babf9053c47cb87a6164d52dc15d673d9f8dc148422e9f5a897e2651
size: 33674
indexed: no
git_status: ??
```

### Handoff evidence

- Run：`${WRITING_MASTER_HOME:-$HOME/.writing-master}/runs/handoff-runtime-acceptance-20260727-final-v3/`
- `effective_status`：`completed`
- `blocking_reasons`：0
- `final.md` SHA-256：`9f4f8a63e3cb12448639ec6fa7ae7b2f23f478530998470348e63ca76c7447a2`
- 实施记录：`docs/proposals/2026-07-27-handoff-runtime-prd.md`。

## 2. Current Workspace At Plan Delivery

```text
?? docs/goals/
?? docs/plans/
?? docs/proposals/2026-07-24-opus5-retrospective-plan.html
```

Goal A 启动时必须重新记录当时的完整状态；不得把本次计划文件误判为未知用户改动，也不得覆盖后来出现的其他用户变更。

## 3. Checkpoints

| Checkpoint | Status | Required evidence |
|---|---|---|
| CP0 Baseline | `verified` | branch、HEAD、51 tests、protected hash/size/index |
| CP1 Store | `verified` | schema、empty state、revision、原子写入、错误测试 |
| CP2 Knowledge | `verified` | 五类、去重身份、搜索相关性、lifecycle、approval |
| CP3 Snapshot | `verified` | Snapshot hash、副本、不变性、verify-run |
| CP4 Standard | `verified` | 真实 Standard 合成 run、marker、usage、final hash |
| CP5 Deep | `verified` | allowed_inputs、全局路径排除、stale 测试 |
| CP6 Closure | `verified` | 全量命令、review、protected file、剩余风险 |

## 4. Task Evidence

### T00 Baseline recheck

Status：`complete`

```text
branch: codex/writing-workflow-maintenance
head: 61dec8cf1e384c752f13a007a1283e9df82fdb95
main: fab9fffcf38c537772dfc8a8b71344a7d33830e5
git_status: ## codex/writing-workflow-maintenance...origin/codex/writing-workflow-maintenance; ?? docs/goals/; ?? docs/plans/; ?? docs/proposals/2026-07-24-opus5-retrospective-plan.html
test_count: 51
test_result: pass (2026-07-28 Goal startup recheck)
protected_sha256: 503ac7b0babf9053c47cb87a6164d52dc15d673d9f8dc148422e9f5a897e2651
protected_size: 33674
protected_indexed: no
protected_git_status: ?? docs/proposals/2026-07-24-opus5-retrospective-plan.html
handoff_effective_status: completed (blocking_reasons: 0)
handoff_final_sha256: 9f4f8a63e3cb12448639ec6fa7ae7b2f23f478530998470348e63ca76c7447a2
```

### T01 Fact alignment

Status：`complete`

```text
changed_docs: README.md; docs/quick-start.md; docs/cli-guide.md; PROJECT_SUMMARY.md; CHANGELOG.md; docs/proposals/2026-07-27-product-capability-prd.md; skills/writing-master/references/agent-orchestration.md; tests/test_documentation_contract.py; tests/test_skill_contracts.py
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_documentation_contract tests.test_skill_contracts -v (pass, 21 tests)
remaining_truth_gaps: Deep Handoff evidence applies only to established deep/multi-agent run directories; quick/standard generic task discovery, recovery and version rollback remain deferred. Goal B/C and generic Task Runtime remain unclaimed.
```

### T10–T12 Store/Profile

Status：`complete`

Required evidence：

- schema versions and canonical empty objects。
- profile revisions `0 → 1 → 2`。
- stale revision error and unchanged latest revision。
- atomic-write/lock/path/corrupt JSON test names。
- `context --help` and CLI exit codes。

T10 evidence：

```text
contract: docs/proposals/2026-07-28-personal-context-foundation.md
fixtures: tests/fixtures/personal-context/{contract-v1.json,orbit-17.md,final.md,acceptance-report.md} (synthetic only)
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context_contract -v (pass, 4 tests)
canonical_empty_profile_sha256: eb7877b5514de357ac7596eb7f894c85985b67f0e1ff39158d1f2cb121351452
canonical_empty_style_sha256: e11311951da1bacb28a2dda57b6c4be4d7f4baffe1a3b6ce07bb770d80ac1ff0
contract_rules: strict UTF-8 raw managed copies; NFC+line-ending normalized hash; canonical JSON allow_nan=False; task-safe Snapshot metadata; frozen approval payload/hash; write-once Snapshot/Usage
failure_codes: not_initialized, invalid_input, invalid_json, schema_unsupported, revision_conflict, path_escape, duplicate, unknown_id, disabled, privacy_unapproved, hash_mismatch, snapshot_conflict
deferred_here: implementation, context CLI, learning/learn CLI, Research Brief
```

T11 evidence：

```text
module: src/writing_master/personal_context.py
public_surface: ContextStore(home).initialize/read_profile/read_style/read_index/locked; canonical_json_bytes; canonical_sha256; safe_path; atomic_write_json; ContextError.code
implementation: stdlib-only fcntl lock, fsync+replace JSON writes, strict JSON parsing, safe path/symlink checks, canonical empty Profile/Style/Index
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context tests.test_personal_context_contract -v (pass, 11 tests)
test_names: initialization idempotence; read-before-init; atomic replace preservation; corrupt JSON; unsupported schema; absolute/parent/symlink escape; independent-process lock serialization
handoff_private_imports: none
```

T12 evidence：

```text
runtime: ContextStore.update_profile(content, expected_revision) validates confirmed-only payloads, preserves an identical current document/revision without writing, and serializes optimistic revision updates under the Context lock.
profile_behavior: empty revision 0 -> ready revision 1 -> changed ready revision 2; same canonical content at current revision is a no-op; every stale expected revision returns revision_conflict before payload validation and preserves the latest document.
cli: writing-master context init; context profile set PROFILE.json --expected-revision N; context profile show [--json]
cli_errors: requested --json returns {"error":{"code":"...","message":"..."}} for Context and argument failures; missing caller PROFILE.json is invalid_input; malformed existing JSON remains invalid_json.
profile_input: exact content fields only (identity, expertise, content_directions, values, expression.tone, avoid, provenance.kind=user_confirmed); runtime owns status/profile_id/revision/updated_at/content_sha256.
storage_hardening: managed reads, locks and atomic writes use an O_DIRECTORY|O_NOFOLLOW anchored directory fd plus dir_fd replace; a coordinated root-path-to-symlink swap test proves the write stays in the originally anchored directory.
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context tests.test_personal_context_contract tests.test_context_cli -v (pass, 23 tests)
cli_smoke: isolated WRITING_MASTER_HOME init/set/show plus missing PROFILE.json and invalid --expected-revision JSON errors (pass; both non-zero invalid_input)
codegraph_sync: pass after T12 changes
diff_check: pass
```

### T20–T22 Knowledge/Privacy

Status：`complete`

Required evidence：

- five kinds and item IDs。
- same-identity duplicate result。
- cross-identity same-hash independent IDs。
- search fixture expected top/inclusion/exclusion/score。
- disabled exclusion and re-enable。
- legacy import twice without duplication。
- private rejection；ask-before-use rejection/approval success；approval scope。

T20 evidence：

```text
runtime: ContextStore.add_material(FILE, kind, title, source_kind, source_ref, visibility, tags, summary) performs strict UTF-8 raw-copy import, canonical normalized-content hash, and identity-local dedupe.
five_kinds: experiences, opinions, cases, references, previous_articles all imported from the synthetic orbit-17 fixture.
isolated_cli_ids: knowledge-3c9504983ba7d341 (experiences/user_provided); knowledge-b7b00bf33cd7cfe0 (opinions/user_confirmed); knowledge-ec7713393d28acdc (cases/user_provided); knowledge-a43586f5076f32f6 (references/external_reference); knowledge-ede67a91dd7ccc70 (previous_articles/user_provided).
index: revision 5 with deterministically sorted IDs; all five managed content.md copies matched the raw fixture SHA-256 3e3c8aa5dea5171718309db46787b177b7f4986ceb1f57de4b631453faa0bd45.
dedupe: same (kind, normalized_content_sha256, source_kind) returns the first metadata without an index revision increment; same body with a different kind or source_kind yields a distinct item ID.
identity_guard: experiences rejects external_reference/editorial_inference; invalid UTF-8 and missing sources return invalid_input.
path_guard: a symlinked managed knowledge/ directory returns path_escape and writes no content outside the store.
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context tests.test_context_cli tests.test_personal_context_contract -v (pass, 28 tests)
codegraph_sync: pass
diff_check: pass
```

T21 evidence：

```text
runtime: list_materials(kind/status), set_material_status(enable/disable), set_material_visibility(expected_revision), and search_materials(query, kind, tag, limit).
lifecycle: status/visibility changes create a new metadata revision; same status is idempotent; stale visibility revision returns revision_conflict and leaves the latest metadata intact.
search_scoring: stdlib NFC/casefold literal matching scores title=10, tags=7, summary=5 and body=2 per matched term; results sort by (-score, item_id), expose metadata plus score only, and never return body text.
behavior_tests: English `orbit agent` selects the expected top item with non-zero score; Chinese `轨道` selects the Chinese fixture; an absent query returns []; tag and kind filters apply; disabled material never appears; equal Signal fixtures use item_id tie-break order.
cli: context material list/disable/enable/set-visibility and context search are registered and validated through the top-level CLI dispatcher.
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context tests.test_context_cli tests.test_personal_context_contract -v (pass, 31 tests)
codegraph_sync: pass
diff_check: pass
```

T22 evidence：

```text
runtime: ContextStore.import_legacy(SOURCE_DIR, kind=None) and ContextStore.approve(RUN_DIR, ITEM_ID, allowed_use), plus reusable admit_material(RUN_DIR, ITEM_ID, purpose) privacy admission.
legacy: initialization does not scan a pre-existing legacy directory; explicit inferred-kind import processed one synthetic file, skipped an identity duplicate, recorded an invalid-UTF-8 per-file failure, and a second import left index revision unchanged.
approval_schema: task-local context-approvals.json has schema_version, task_id, revision, append-only approvals, approved_at and approval_sha256; repeating an identical (task_id, item_id, allowed_use) returns the original approval without revision change.
privacy_admission: publishable returns {status:not_required}; ask_before_use rejects before matching approval and succeeds only for the exact task/item/purpose; private returns privacy_unapproved; disabled returns disabled.
cli: context import-legacy SOURCE_DIR [--kind KIND] and context approve RUN_DIR ITEM_ID --allow background|paraphrase|quote are registered through the top-level dispatcher.
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context tests.test_context_cli tests.test_personal_context_contract -v (pass, 34 tests)
codegraph_sync: pass
diff_check: pass
```

### T30 Snapshot/Verify

Status：`complete`

Required evidence：

```text
task_id:
profile_status_revision_hash:
style_status_revision_hash:
material_ids:
material_metadata_hashes:
material_content_hashes:
approval_ids:
snapshot_path:
snapshot_sha256:
context_usage_path:
verify_run_result:
old_snapshot_after_global_update:
```

T30 evidence：

```text
runtime: create_snapshot(RUN_DIR, materials=[(ITEM_ID, PURPOSE)]), record_usage(RUN_DIR, uses, artifact_paths), and verify_run(RUN_DIR); CLI surfaces context snapshot and context verify-run.
snapshot_schema: task_id, RFC3339 created_at, frozen Profile/Style status+revision+content hash, sorted materials, task-safe metadata projection/hash, raw content hash, purpose, frozen approval/not_required value, copy_path and snapshot_sha256.
immutability: same material pairs return the original Snapshot regardless of later global changes; different pairs return snapshot_conflict; tests mutate Profile and a material visibility after Snapshot and assert old bytes/copies remain unchanged while a later task sees the newer profile revision.
admission: private, disabled and missing/mismatched ask_before_use approval are rejected; publishable is admitted with {status:not_required}.
usage_verify: Context Usage is write-once/idempotent for identical uses+artifact hashes, requires Snapshot item/purpose pairs, and verify-run recomputes Snapshot copy, final and acceptance hashes; a tampered final returns hash_mismatch.
focused_tests: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context tests.test_context_cli tests.test_personal_context_contract -v (pass, 37 tests)
codegraph_sync: pass
diff_check: pass
```

### T31 Standard Agent synthetic run

Status：`complete`

Synthetic data only：`required`

```text
run_path: /tmp/awm-goal-a-standard-synthetic-20260728.XrjDOs/home/runs/standard-synthetic-20260728
mode: standard
execution: single_agent
fixture_item_id: knowledge-3c9504983ba7d341
unique_marker: ORBIT-17
snapshot_sha256: 9e33f6cd80961046e2fa61f8f52ef2a6ed05b4ed73557fe225e18bb43973e4b6
snapshot_file_sha256: a7a9fbd88f060857dc2ba8572cc9bc6ffa762f9de18ea62aa7dc82645d2f7596
context_usage_sha256: ff513c51c0232acd627e50e1991d8ac6947106fb5b608227fd1c8f774e7725c7
final_sha256: 0077b186ea9c4ef9e81852c34c21728c2a3ea0f105ec2651c4fd69a5e2f2ef3b
acceptance_sha256: 67e75ee76f23977d1ee702ff3e7703bf9b636305e7806da329bd15f13a999cd9
marker_verified_in_final: yes; exactly one occurrence
usage_links_item: yes; background/opening/claim-orbit-17
verify_run_result: pass; verified=true
```

真实 Standard 单 Agent 仅读取该 run 的 `brief.md`、Snapshot 和任务内材料副本，产出完整核心交付文件与 Runtime 写入的 usage；该合成 run 只证明工程链路，不证明产品效果。

### T32 Deep Handoff

Status：`complete`

```text
manifest_fixture: tests.test_handoff.HandoffTests.test_deep_context_manifest_is_task_local_and_snapshot_tamper_is_stale
allowed_input_paths: writer=[brief.md, personal-context-snapshot.json, context-materials/knowledge-synthetic.md]; auditor=[draft-v1.md, personal-context-snapshot.json, context-materials/knowledge-synthetic.md]
global_personal_context_path_absent: yes; exact task-relative allowed_inputs excludes the created external home/personal-context fixture
parent_conversation_absent: yes; Writer/Auditor forbidden_inputs=[parent_conversation], and allowed_inputs contains only declared task files
snapshot_hash: 8d49aa566dea7184e4d566202688ac5ddd37de494bac4925b6d11e9c0b5f3b87 before tamper
tamper_action: overwrite personal-context-snapshot.json frozen payload with a distinct task-local payload after the Auditor Manifest is prepared
effective_status_after_tamper: stale
blocking_reason: input hash changed: personal-context-snapshot.json
```

测试同时验证 Writer/Auditor 每项 Manifest input 都是 required 且 SHA-256 等于准备时任务内文件的 hash；结论只覆盖 Host 输入构造合同和 Runtime hash/stale，不写“Agent 受到 OS 文件隔离”。

### T33 Closure

Status：`complete`

```text
isolated_home: /tmp/awm-goal-a-t33-close-20260728.UlPutc/home
behavior_smoke: pass; init; profile revision 0→1→2; experiences/user_provided and opinions/user_confirmed retain distinct IDs with the same normalized content hash; query search; disable/enable exclusion; duplicate legacy file skipped and distinct synthetic legacy file imported; task approval; Snapshot; write-once Usage; verify-run=true
smoke_item_ids: experience=knowledge-eb4c671c8cf5e328; opinion=knowledge-7d4f0289033c7b86; legacy=knowledge-3bc4d8b06eb01744
smoke_snapshot_sha256: e1c9eb9546064ea42188705ddfc372cb8683591b24adb13597ce5d194356e32f
full_tests: 95/95 pass (2026-07-28 final command set)
compileall: pass (`PYTHONPYCACHEPREFIX=/tmp/awm-pyc-goal-a-final .venv/bin/python -m compileall -q src tests`)
install_syntax: pass (`bash -n install.sh`)
installer_contract: `tests.test_install_contract`, 3/3 pass; repository-root CLI install and existing Skill-directory preservation are covered, without silent legacy import
cli_help: `./bin/writing-master --version` => 1.0.0; top-level help contains context; `context --help` contains snapshot
build_artifacts: /tmp/awm-goal-a-build-20260728.8g9vBu/{writing_master-1.0.0-py3-none-any.whl,writing_master-1.0.0.tar.gz}
markdown_links: `tests.test_documentation_contract`, 6/6 pass
diff_check: pass
codegraph_sync: pass (already up to date)
review_report: docs/goals/2026-07-28-v0.2a-review.md
open_blocker_major: none; M1/M2 resolved by fresh Sol re-review
protected_sha256_final: 503ac7b0babf9053c47cb87a6164d52dc15d673d9f8dc148422e9f5a897e2651
protected_size_final: 33674
protected_indexed_final: no
protected_git_status_final: ?? docs/proposals/2026-07-24-opus5-retrospective-plan.html
```

## 5. Evidence Log

只追加，不覆盖历史。

| Time | Task | Command / Artifact | Result | Notes |
|---|---|---|---|---|
| 2026-07-28 | planning baseline | `.venv/bin/python -m unittest discover -s tests -v` | pass, 51 tests | 计划制定前基线 |
| 2026-07-28 | planning validation | documentation tests, compileall, `bash -n`, temporary `uv build`, `git diff --check`, `codegraph sync` | pass | 仅验证计划没有破坏现有仓库 |
| 2026-07-28 | T00 baseline recheck | branch/HEAD/main, `git status --short --branch`, protected SHA-256/size/index/status, Handoff `show`, full unittest | pass, 51 tests | Protected HTML remains untracked and byte-identical to the recorded baseline. |
| 2026-07-28 | T01 fact alignment | documentation/skill contract tests | pass, 21 tests | Deep Handoff acceptance is scoped to deep/multi-agent runs; generic quick/standard recovery remains deferred. |
| 2026-07-28 | T10 data contract | contract, synthetic fixture bodies and schema test | pass, 4 tests | Canonical empty/Profile hashes, raw/normalized material hashes, approval/projection/snapshot hashes and stable failure codes are fixed before Runtime implementation. |
| 2026-07-28 | T11 Context Store | stdlib ContextStore and focused storage/contract tests | pass, 11 tests | Root/path/lock/atomic JSON/corrupt-schema behavior is covered without importing Handoff internals. |
| 2026-07-28 | T12 Profile/context CLI | revisioned Author Profile, `context init/profile set/profile show`, JSON error behavior and anchored-FD storage regression | pass, 23 focused tests | Isolated CLI smoke proves `0 → 1 → 2`, stale preservation, parser/input JSON errors, and no-op semantics; root-symlink swap remains inside the anchored managed directory. |
| 2026-07-28 | T20 managed materials | five-kind import, raw managed copies, identity-local dedupe and `context material add` | pass, 28 focused tests | Synthetic fixture CLI import produced five distinct IDs, index revision 5 and byte-identical managed bodies; invalid identity/UTF-8/symlink paths are rejected. |
| 2026-07-28 | T21 lifecycle/search | metadata-only list, enable/disable, visibility revision and deterministic bilingual search | pass, 31 focused tests | Fixed English/Chinese queries prove query-dependent inclusion/top rank/score, disabled exclusion, filters and stable equal-score order. |
| 2026-07-28 | T22 legacy/approval | explicit legacy import, task-local approval log and deterministic privacy admission | pass, 34 focused tests | Existing legacy files remain untouched until explicit import; duplicate/failing files are reported independently; private, disabled and unapproved admissions are rejected. |
| 2026-07-28 | T30 Snapshot/verify | immutable Snapshot/copies, write-once Context Usage and hash-based run verification | pass, 37 focused tests | Profile/material global changes do not rewrite an established task; verify-run rejects tampered final artifacts. |
| 2026-07-28 | T31 Standard integration | `tests.test_skill_contracts` + `tests.test_documentation_contract`; isolated real Standard synthetic run; `context verify-run` | pass, 22 contract/doc tests; verified=true | The single-agent run used task-local synthetic marker `ORBIT-17` exactly once in `final.md`, recorded `knowledge-3c9504983ba7d341` as `background/opening/claim-orbit-17`, and produced hash-linked final/acceptance/usage artifacts. |
| 2026-07-28 | T32 Deep boundary | fake-host Writer/Auditor Snapshot inputs plus stale mutation; skill/doc contracts | pass, `tests.test_handoff` + `tests.test_skill_contracts` + `tests.test_documentation_contract` (46 tests) | Both Manifests enumerate only task-local files with recomputed hashes; an Auditor prepared against a frozen Snapshot becomes stale after Snapshot tampering. This proves Host construction/hash behavior, not OS isolation. |
| 2026-07-28 | T33 isolated closure smoke | `/tmp/awm-goal-a-t33-close-20260728.UlPutc/home` | pass | Synthetic init, Profile `0→1→2`, cross-identity same-hash materials, search, lifecycle, explicit legacy import, approval, Snapshot, Usage and `verify-run=true`. |
| 2026-07-28 | T33 final command set | full unittest, compileall, install syntax, installer contract (3/3), CLI, Markdown links, temporary `uv build`, diff check, CodeGraph sync, protected-file checks | pass, 95/95 tests | Build output: `/tmp/awm-goal-a-build-20260728.8g9vBu`; protected file remained byte-identical, untracked and out of index. |
| 2026-07-28 | T33 independent review | `docs/goals/2026-07-28-v0.2a-review.md` | pass | Fresh Sol reviewer resolved M1 Context Usage concurrency and M2 Home symlink identity; Open Blocker/Major: none. |

## 6. Independent Review

- Required report：`docs/goals/2026-07-28-v0.2a-review.md`
- Reviewer：fresh Sol reviewer，非主体代码作者
- Review date：2026-07-28（初审与 M1/M2 复审）
- Reviewed HEAD/diff：`61dec8cf1e384c752f13a007a1283e9df82fdb95` 之上的当前未提交 Goal A 工作区差异
- Author independence：reviewer 未参与主体代码编写；报告由独立 fresh Sol review 写入固定路径

| Severity | Location | Finding | Resolution | Verification |
|---|---|---|---|---|
| Major M1 | `personal_context.record_usage()` | Context Usage check-then-write 竞争 | run-local anchored-FD `flock` 覆盖完整读取、校验和首次写入临界区 | 双进程竞争测试；95/95 全量测试 |
| Major M2 | `ContextStore` Home path | Home 被重定向后可能发生身份漂移 | 固定 resolved Home inode identity，并经 anchored Home FD 打开受管路径 | Home rename/external-symlink 回归测试；95/95 全量测试 |

Open Blocker/Major：`none`

Reviewer 输入固定为：Goal 合同、Program 方案、Goal A checklist、实现 diff、测试输出和本验收记录。Reviewer 不参与主体代码编写。

## 7. Final Validation

| Validation | Command | Result |
|---|---|---|
| Unit tests | `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest discover -s tests -v` | pass, 95/95 |
| Compile | `PYTHONPYCACHEPREFIX=/tmp/awm-pyc-goal-a-final .venv/bin/python -m compileall -q src tests` | pass |
| Install syntax | `bash -n install.sh` | pass |
| Installer contract | `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_install_contract -v` | pass, 3/3 |
| CLI | `./bin/writing-master --version`、`--help`、`context --help` | pass; version `1.0.0` |
| Build | `uv build --out-dir "$OUT"` | pass; wheel + sdist in `/tmp/awm-goal-a-build-20260728.8g9vBu` |
| Markdown links | `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_documentation_contract -v` | pass, 6/6 |
| Diff whitespace | `git diff --check` | pass |
| CodeGraph | `codegraph sync` | pass; already up to date |
| Protected file | SHA-256/size/index/status | pass; baseline hash/33674/no/`??` unchanged |
| Workspace | `git status --short --branch` | branch `codex/writing-workflow-maintenance`; only current Goal A changes plus the preserved protected untracked HTML |

## 8. Deferred

以下保持未实现：

- Goal B：确认式风格学习和 `learn` CLI。
- Goal C：Research Brief 与上下文感知选题。
- quick/standard 通用 Task Runtime、恢复和版本回退。
- CI hosted run、分支合并、发布和版本决策。
- Web UI、云同步、向量库、多用户、新平台、自动发布、数据增长闭环。

## 9. Completion Decision

- Final status：`complete`
- Done condition reviewer：verified against Goal contract §2: A-GC-01…12, T00–T33, Runtime/Standard/Deep behavior, final commands, independent review and protected-file checks are evidenced above.
- Completion date：2026-07-28
- Commit：`not_created`
- Branch：`codex/writing-workflow-maintenance`
- Remaining risks：no open Blocker/Major. Reviewer m2 (duplicated task-ID validation) is a non-blocking maintenance suggestion; Goal B/C, generic quick/standard Task Runtime, CI and release remain explicitly deferred.

`complete` 记录的是本 Goal 的证据结论；Goal 状态由完成时的 Codex Goal 更新单独同步。证据日志的本次更新不改变 Runtime 或合同语义。
