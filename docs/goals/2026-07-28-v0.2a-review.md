# Goal A 独立审查：Personal Context Foundation

- 初审日期：2026-07-28
- M1/M2 复审日期：2026-07-28
- Reviewer：fresh Sol，非主体代码作者
- Reviewed HEAD：`61dec8cf1e384c752f13a007a1283e9df82fdb95`
- Reviewed state：上述 HEAD 之上的当前未提交 Goal A 工作区差异
- 最终结论：**M1、M2 均已关闭；Open Blocker/Major: none。**

## 1. 审查范围

初审固定输入：

1. `docs/goals/2026-07-28-v0.2-goal-contract.md`
2. `docs/plans/2026-07-28-project-advancement-plan.md`
3. `docs/plans/2026-07-28-v0.2-execution-checklist.md`
4. 当前 `git diff --`、`git status --short --branch`
5. `docs/goals/2026-07-28-v0.2-acceptance.md`
6. T31 Standard synthetic run 与 T32 Deep Manifest/stale 证据

初审覆盖 schema/empty/revision/atomic/path、素材去重与来源身份、privacy/approval/purpose、Snapshot/usage/verify、Standard 实际素材使用、Deep Manifest 的 task-local input/hash/stale 边界、CLI/installer/docs 范围及测试覆盖。

本次复审严格限定为初审的两个 Major：

- M1：Context Usage 并发 write-once 竞争。
- M2：ContextStore Home symlink retargeting/path identity。

## 2. 当前命令与证据

| 检查 | 当前结果 |
|---|---|
| `codegraph explore ...`（M1/M2 当前源码、调用路径与新测试） | 已执行；CodeGraph 返回当前磁盘源码 |
| `git status --short --branch` / 当前差异 / `git diff --check` | 已复核；diff check pass |
| 两个新定向测试 | pass；连续执行 5 轮均通过 |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_personal_context -v` | **24/24 pass** |
| `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest discover -s tests -v` | **95/95 pass** |
| `PYTHONPYCACHEPREFIX=/tmp/awm-review-pyc2 .venv/bin/python -m compileall -q src tests` | pass |
| `bash -n install.sh` | pass |
| `codegraph sync` | pass；already up to date |

初审的其他验收证据仍成立：T31 run 的 `ORBIT-17` 在 `final.md` 恰好出现一次，snapshot/final/acceptance/usage hash 与验收记录一致，`verify-run` 返回 `verified=true`；T32 的 task-local Manifest/hash/stale 证据没有夸大为 OS 级文件隔离；受保护 HTML 的已审指纹为 SHA-256 `503ac7b0babf9053c47cb87a6164d52dc15d673d9f8dc148422e9f5a897e2651`、size `33674`、indexed `no`、status `??`。

## 3. M1/M2 复审

### Blocker

| ID | 结果 |
|---|---|
| — | **none** |

### Major resolution

| ID | 修正证据 | 复审判断 | 状态 |
|---|---|---|---|
| M1 | `src/writing_master/personal_context.py:498-511` 新增 run-local `.context-usage.lock`，以 anchored `run_fd`、`O_NOFOLLOW` 和 `flock(LOCK_EX)` 串行化竞争写入；`record_usage()` 在 `1492-1526` 将 task/snapshot/artifact 计算、既有 Usage 检查和首次写入全部置于同一临界区。`tests/test_personal_context.py:618-652` 启动两个进程写入不同 `uses`，断言结果恰为一方成功、一方 `duplicate`，且落盘记录等于成功方并通过 `verify_run()`。 | 修正覆盖了原竞态的完整 check-then-write 区间。不同 Usage 不再能同时成功或互相覆盖；幂等相同 Usage 的既有行为保留。符合 write-once、失败不静默推进和 Runtime 锁/确定性 invariant。 | **resolved** |
| M2 | `ContextStore` 在 `src/writing_master/personal_context.py:910-937` 增加 Home 解析目标检查、anchored Home FD，以及 `(st_dev, st_ino)` identity 固定；所有 canonical store 读写经 `_root_fd()` / `_locked_root()` 进入受检路径。`tests/test_personal_context.py:259-279` 在已初始化 Store 上 rename Home 并用外部 symlink 替换，断言更新返回 `path_escape`、原 Profile 不变、外部目录没有写入；既有 root-path-swap FD 测试继续通过。 | 修正同时处理符号链接目标变化与目录 identity 变化，并从 Home FD 逐段打开 `personal-context`。原复现路径现在在任何 Profile 写入前被拒绝，符合 symlink escape、上一有效状态保留和失败不推进 invariant。 | **resolved** |

## 4. 剩余发现

### Open Blocker/Major

**none**

本次 M1/M2 复审没有发现由修正引入的新 Blocker 或 Major。

### 初审 Minor 状态说明

- 初审 m1（零字节素材行为未固定）在当前差异中已有明确 `invalid_input` 检查与回归断言；该项现已消除。
- 初审 m2（`approve()` / `admit_material()` 未复用 `_task_id_at()`）仍是非阻断维护性建议；本次按要求未扩大复审范围处理它。

## 5. 最终结论

**Open Blocker/Major: none**

就 Goal A 合同及本次独立复审范围而言，M1/M2 修正满足锁、write-once、路径、symlink escape、状态保留和失败不推进 invariants。当前没有阻止 Goal A 关闭的审查发现；m2 仅为可选的非阻断维护项。
